

class GridBoardEngine {
	constructor(index) {
		this.index = index;
	}
	
	
	
}

// TEST ZONE
var gb = new GridBoardEngine(1);	// 처음 설정할 인덱스 번호를 0으로 시작할지, 1로 시작할지 설정한다.
gb.	// width와 height를 각각 x,y로 대입해 새로운 보드를 만든다. (새로운 또 다른 보드를 만들려면, 새로운 클래스를 생성해야 한다)
gb.	// 원한다면 전체 값을 
